# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a personal portfolio site for Ashmit Rama — currently a single static `index.html` file with no build tooling, frameworks, or dependencies.

## Development

Open `index.html` directly in a browser to preview. There is no build step, dev server, or package manager.

## Structure

The entire site is `index.html`. As the site grows, the natural next steps would be adding a CSS file (`styles.css`) and a JavaScript file (`main.js`) linked from `index.html`, or migrating to a static site generator.
